import GlobalStyle from './base/global-style';
import {color, fontSize, box } from './abstract/theme';
import mixins from './abstract/mixins';


// export only what is for external use
export {
    GlobalStyle,
    color,
    box,
    mixins,
    fontSize
}